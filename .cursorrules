Follow AGENTS.md. Never write directly to .hasna/apps/knowledge or generated
artifact files. Use the knowledge CLI, knowledge-mcp, or the @hasna/knowledge
SDK, then run knowledge storage validate --strict --scope project --json after
knowledge state changes.
